import { defineConfig } from 'vite'

export default defineConfig({
  // When deploying to GitHub Pages under a repository name, set the base
  // so asset URLs include the repo path. Update if your repo name changes.
  base: '/slime-knight/'
})
